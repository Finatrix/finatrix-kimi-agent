{
  // Cloudflare Worker that serves the built React app as a static
  // single-page application. No server code — just the files in ./dist.
  "name": "finatrix",
  "compatibility_date": "2024-11-01",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
