/**
 * RETIRED. The old iframe-based tools host (which embedded public/tools-app.html)
 * has been replaced by the native React tools shell in `src/tools/ToolsLayout.tsx`
 * with per-tool routes in `src/tools/ToolRoute.tsx`.
 *
 * This file is no longer imported anywhere. It is kept only because this
 * environment cannot delete files; it is safe to `git rm src/pages/ToolsPage.tsx`.
 */
export {};
