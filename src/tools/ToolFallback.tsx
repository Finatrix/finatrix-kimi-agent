/**
 * RETIRED. This was transitional scaffolding that embedded not-yet-migrated
 * tools from public/tools-app.html inside an iframe during the V4 migration.
 * All seven tools are now native React pages (see `src/tools/pages/`), so the
 * fallback is unused.
 *
 * No longer imported anywhere. Safe to `git rm src/tools/ToolFallback.tsx`.
 */
export {};
