[build]
  command = "npm run build:netlify"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so client-side routes (react-router) resolve to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
